# Web Design System — Money Space

> **Superseded.** The current design system is
> **[family-finance-v3.1/design.md](family-finance-v3.1/design.md)**.

This file used to hold the v2 system, which centered Home on `snapshot + bills + discussion`.
v3.1 explicitly retires that layout: Home is now centered on **Financial State → Flexible Money →
What-if → 30 Days Ahead → Money Location → Main Goal → Freshness**.

The v3.1 document restates every shared visual primitive (tokens, type scale, spacing, radius,
shadows, the section → sub-section → metric IA), so nothing from v2 is lost by using it alone.

The full v2 text remains in git history:

```bash
git show 28bb7c5:design.md
```

Other superseded v1/v2 documents at this repo root — `# Product Spec v1.md`,
`# Backend Tables & Relationships — Money.md`, `Money Space — Apple-like Design System v2.md` —
are likewise history, not current guidance. The current spec set is [family-finance-v3.1/](family-finance-v3.1/).
